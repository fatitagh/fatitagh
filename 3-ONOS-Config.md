برای پیکربندی ONOS به‌عنوان کنترلر SDN و اطمینان از اینکه روی پورت 6653 (پورت استاندارد OpenFlow) گوش می‌دهد، مراحل زیر را به‌صورت گام‌به‌گام انجام دهید. این راهنما فرض می‌کند که شما ONOS را در یک کانتینر داکر یا به‌صورت مستقل نصب کرده‌اید.

### پیش‌نیازها
- ONOS نصب‌شده (ترجیحاً از طریق داکر با ایمیج `onosproject/onos`).
- دسترسی به خط فرمان یا رابط وب ONOS.
- دانش پایه در مورد پروتکل OpenFlow و مدیریت کانتینرهای داکر.

---

### گام ۱: راه‌اندازی کانتینر ONOS
اگر از داکر استفاده می‌کنید، کانتینر ONOS را با پورت‌های مورد نیاز اجرا کنید:
```bash
docker run -t -d -p 8181:8181 -p 6653:6653 --name onos onosproject/onos
```
- **پورت 8181**: برای رابط وب ONOS.
- **پورت 6653**: برای پروتکل OpenFlow جهت ارتباط با سوییچ‌ها (مثلاً Mininet).
- بررسی کنید که کانتینر در حال اجرا باشد:
```bash
docker ps
```
- آدرس IP کانتینر را پیدا کنید (برای اتصال Mininet یا سایر ابزارها):
```bash
docker inspect onos | grep IPAddress
```
فرض کنید IP کانتینر مثلاً `172.17.0.2` باشد.

---

### گام ۲: فعال‌سازی پروتکل OpenFlow در ONOS
برای اطمینان از اینکه ONOS روی پورت 6653 گوش می‌دهد، باید برنامه OpenFlow در ONOS فعال شود.

1. **ورود به خط فرمان ONOS**:
   به کانتینر ONOS دسترسی پیدا کنید:
   ```bash
   docker exec -it onos bash
   ```
   سپس به رابط خط فرمان ONOS وارد شوید:
   ```bash
   onos
   ```
   یا مستقیماً:
   ```bash
   docker exec -it onos onos
   ```

2. **فعال‌سازی برنامه OpenFlow**:
   در خط فرمان ONOS، برنامه OpenFlow را فعال کنید:
   ```bash
   app activate org.onosproject.openflow
   ```
   این برنامه اطمینان می‌دهد که ONOS روی پورت 6653 برای پروتکل OpenFlow گوش می‌دهد.

3. **بررسی وضعیت برنامه**:
   برای تأیید فعال بودن برنامه OpenFlow:
   ```bash
   apps -s -a
   ```
   باید `org.onosproject.openflow` در لیست برنامه‌های فعال باشد.

---

### گام ۳: پیکربندی پورت OpenFlow
به‌طور پیش‌فرض، ONOS روی پورت 6653 برای OpenFlow گوش می‌دهد، اما می‌توانید این تنظیم را بررسی یا تغییر دهید:

1. **فایل پیکربندی ONOS**:
   فایل پیکربندی OpenFlow در مسیر زیر قرار دارد:
   ```
   /opt/onos/config/network-cfg.json
   ```
   داخل کانتینر، فایل را بررسی کنید:
   ```bash
   cat /opt/onos/config/network-cfg.json
   ```
   بخش مربوط به OpenFlow ممکن است چیزی شبیه به این باشد:
   ```json
   {
     "openflow": {
       "ports": {
         "of:0000000000000001": {
           "interfaces": [
             {
               "name": "s1",
               "ofport": 6653
             }
           ]
         }
       }
     }
   }
   ```
   اطمینان حاصل کنید که پورت 6653 در تنظیمات ذکر شده است. اگر نیاز به تغییر دارید، فایل را ویرایش کنید:
   ```bash
   nano /opt/onos/config/network-cfg.json
   ```
   سپس ONOS را ری‌استارت کنید:
   ```bash
   docker restart onos
   ```

2. **بررسی پورت‌های باز**:
   برای تأیید اینکه ONOS روی پورت 6653 گوش می‌دهد، از ابزار `netstat` یا `ss` در کانتینر استفاده کنید:
   ```bash
   docker exec -it onos netstat -tuln | grep 6653
   ```
   خروجی باید چیزی شبیه به این باشد:
   ```
   tcp        0      0 0.0.0.0:6653            0.0.0.0:*               LISTEN
   ```

---

### گام ۴: اتصال Mininet به ONOS
برای تست اینکه ONOS به‌عنوان کنترلر روی پورت 6653 کار می‌کند، Mininet را به ONOS متصل کنید:

1. **راه‌اندازی Mininet**:
   کانتینر Mininet را اجرا کنید (اگر هنوز اجرا نشده):
   ```bash
   docker run -it --privileged --name mininet iwaseyuke/mininet
   ```

2. **ایجاد یک شبکه ساده**:
   در Mininet، یک شبکه با یک سوییچ و دو هاست ایجاد کنید و آن را به ONOS متصل کنید:
   ```bash
   mn --topo=single,2 --controller=remote,ip=172.17.0.2,port=6653
   ```
   - جایگزین `172.17.0.2` با IP واقعی کانتینر ONOS کنید.

3. **تست اتصال**:
   در Mininet، دستور زیر را اجرا کنید تا اتصال بین هاست‌ها را بررسی کنید:
   ```bash
   mininet> pingall
   ```
   اگر خروجی نشان دهد که پینگ‌ها موفق هستند، ONOS به‌درستی به‌عنوان کنترلر روی پورت 6653 کار می‌کند.

---

### گام ۵: بررسی وضعیت از طریق رابط وب ONOS
1. به رابط وب ONOS در مرورگر دسترسی پیدا کنید:
   ```
   http://<IP سرور داکر>:8181/onos/ui
   ```
   (مثلاً `http://172.17.0.2:8181/onos/ui`)

2. **ورود**:
   - نام کاربری: `onos`
   - رمز عبور: `rocks`

3. **بررسی توپولوژی**:
   - به بخش **Topology** بروید تا سوییچ‌ها و هاست‌های متصل را ببینید.
   - در بخش **Devices**، سوییچ‌های OpenFlow (مثلاً s1) را بررسی کنید.
   - در بخش **Flows**، قوانین جریان ایجادشده توسط ONOS برای پورت 6653 را مشاهده کنید.

---

### گام ۶: عیب‌یابی
- **پورت 6653 باز نیست**:
  - بررسی کنید که فایروال پورت 6653 را مسدود نکرده باشد:
    ```bash
    sudo ufw allow 6653
    ```
  - اطمینان حاصل کنید که برنامه OpenFlow در ONOS فعال است (گام ۲ را دوباره بررسی کنید).

- **اتصال Mininet به ONOS برقرار نمی‌شود**:
  - مطمئن شوید که IP و پورت کنترلر در Mininet درست وارد شده‌اند.
  - بررسی کنید که کانتینر ONOS در حال اجرا باشد:
    ```bash
    docker ps
    ```

- **خطای پروتکل OpenFlow**:
  - نسخه OpenFlow در Mininet و ONOS باید سازگار باشد (معمولاً OpenFlow 1.3). برای تنظیم نسخه در Mininet:
    ```bash
    mn --topo=single,2 --controller=remote,ip=172.17.0.2,port=6653 --switch ovs,protocols=OpenFlow13
    ```

---

### خروجی نهایی
- ONOS روی پورت 6653 برای پروتکل OpenFlow پیکربندی شده و گوش می‌دهد.
- Mininet به‌عنوان یک شبکه SDN به ONOS متصل شده و اتصال با `pingall` تأیید شده است.
- رابط وب ONOS توپولوژی شبکه و قوانین جریان را نمایش می‌دهد.

اگر نیاز به جزئیات بیشتر یا پیکربندی پیشرفته‌تر (مثلاً برای O-RAN) دارید، لطفاً اطلاع دهید!